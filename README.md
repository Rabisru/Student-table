# Student-table
Full CRUD — Add, Edit, Delete with in-memory state (no backend needed) Form validation — All fields required, valid email regex, age 16–100 Pre-filled Edit form — Opens with existing data Delete confirmation dialog — Modal with name confirmation Simulated loading state — 700ms shimmer bar on every mutation
